on:
  push:
    branches:
      - tests
      
defaults:
  run:
    working-directory: ./src/tests/googletest

name: GoogleTest

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
    - uses: actions/checkout@v2

    - name: Configure
      run: |
        cd /home/runner/work/FIMS-software
        cmake -S . -B build 
        
    - name: Build
      run: |
        cd build 
        cmake --build .
        
    - name: Test
      run: |
        ctest 